# Repo root — backend API (set Root Directory empty OR "backend" with backend/railway.toml)
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health/db"
restartPolicyType = "on_failure"
